<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/brand_pink_bg"
    android:fitsSystemWindows="true"
    tools:context=".ui.main.MainActivity">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_container"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_root" />

    <!-- Floating pill containing our CUSTOM bottom nav.
         wrap_content auto-sizes to the content; no dead space possible. -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/bottom_bar_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="88dp"
        android:layout_marginBottom="@dimen/bottom_bar_margin"
        android:backgroundTint="@color/surface_white"
        app:cardBackgroundColor="@color/surface_white"
        app:cardCornerRadius="36dp"
        app:cardElevation="6dp"
        app:strokeWidth="0dp">

        <include
            android:id="@+id/bottom_nav"
            layout="@layout/view_bottom_nav" />
    </com.google.android.material.card.MaterialCardView>

    <!-- Circular search button.
         Explicit 64×64dp: cornerSize(32dp) == width/2 == PERFECT circle.
         Same marginBottom as the pill = bottom edges flush. -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_search"
        style="@style/Widget.Artistico.Fab.Search"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="12dp"
        android:layout_marginBottom="@dimen/bottom_bar_margin"
        android:contentDescription="@string/nav_search"
        app:elevation="6dp"
        app:fabCustomSize="64dp"
        app:maxImageSize="24dp"
        app:srcCompat="@drawable/ic_search"
        app:tint="@color/text_primary" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
